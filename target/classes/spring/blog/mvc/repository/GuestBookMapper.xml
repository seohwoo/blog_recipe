<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.blog.mvc.repository.GuestBookMapper">
	<!-- 	
	<insert id="write" parameterType="spring.blog.mvc.bean.GuestBookDTO">
		insert into guestbook (num, content, reg_date) values(
				guestbook_seq.NEXTVAL, #{content}, sysdate)
	</insert>
	<select id="bookList" resultType="spring.blog.mvc.bean.GuestBookDTO">
		select * from guestbook order by reg_date desc
	</select>
	-->
	
	<insert id="guestBookInsert" parameterType="">
		insert into board (num, writer, title, content, reg_date, readcount, boardnum, files, tablenum)
		values(board_seq.NEXTVAL, #{writer}, #{title}, #{content}, sysdate, #{readcount}, 
		#{boardnum}, #{files}, #{tablenum} )
	</insert>
</mapper>